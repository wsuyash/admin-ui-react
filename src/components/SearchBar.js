const SearchBar = () => {
	return (
		<div className="SearchBar">
			<input className="w-full p-2 border-2 border-gray-300" placeholder="Search by name, email, or role" />
		</div>
	);
}

export default SearchBar;